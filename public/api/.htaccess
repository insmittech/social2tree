<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /api/

  # Remove .php extension for cleaner URLs
  # Example: /api/auth/login instead of /api/auth/login.php
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME}\.php -f
  RewriteRule ^(.*)$ $1.php [L]
</IfModule>
